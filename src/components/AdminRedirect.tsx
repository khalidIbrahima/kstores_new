'use client'

// This component is intentionally a no-op.
// Admin redirect only happens after login, not on every page navigation.
// See login/page.tsx for the redirect logic.
export default function AdminRedirect() {
  return null
}
